name: Release Plugin

on:
  push:
    branches: [ main ]

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Read plugin version
        id: version
        run: |
          VERSION=$(grep -m1 "Version:" bricks-builder-mcp.php | sed 's/.*Version:[[:space:]]*//' | tr -d '[:space:]')
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          echo "Plugin version: $VERSION"

      - name: Build plugin zip
        run: |
          # WP requires the zip to extract into a folder named after the plugin.
          # Repo root IS the plugin — copy files into bricks-builder-mcp/ then zip.
          mkdir -p dist/bricks-builder-mcp
          cp -r admin assets includes bricks-builder-mcp.php uninstall.php dist/bricks-builder-mcp/
          cd dist && zip -r bricks-builder-mcp.zip bricks-builder-mcp/
          echo "Zip size: $(du -sh bricks-builder-mcp.zip | cut -f1)"

      - name: Publish or update GitHub release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          TAG="v${{ steps.version.outputs.version }}"
          NOTES="$(git log -1 --format='%s%n%n%b' HEAD | head -20)"

          if gh release view "$TAG" > /dev/null 2>&1; then
            # Release for this version already exists — update the zip and notes
            gh release upload "$TAG" dist/bricks-builder-mcp.zip --clobber
            gh release edit   "$TAG" --notes "$NOTES" --latest
            echo "Updated existing release $TAG"
          else
            gh release create "$TAG" dist/bricks-builder-mcp.zip \
              --title "Bricks Builder MCP $TAG" \
              --notes "$NOTES" \
              --latest
            echo "Created new release $TAG"
          fi
